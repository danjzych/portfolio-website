'use strict';

/** Matches tailwind breakpoints. Used to determine whether certain *component*
 * are rendered or not.
 */
export enum Breakpoints {
	sm = 640,
	md = 768,
	lg = 1024,
	xl = 1280,
	'2xl' = 1536,
}
